This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
auth.php
cambiar_password.php
cerrar_session.php
conexion.php
css/dashboard.css
css/estilos.css
fotos/usuario_7.jpeg
get_distritos.php
get_provincias.php
img/fgeneral.png
img/img_principal.png
index.php
login_mesa.php
p_cambiar_password.php
p_login_mesa.php
p_login.php
p_perfil_foto.php
p_perfil.php
p_registro.php
perfil_foto.php
perfil.php
principal_mesa.php
principal.php
registro.php
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="cambiar_password.php">
<?php
session_start();

if (!isset($_SESSION["auth"]) || $_SESSION["auth"] != "1") {
    header("Location: index.php");
    exit();
}
?>

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cambiar Contraseña | UNJFSC</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/dashboard.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-brand">UNJFSC <span>| Seguridad</span></div>
        <div class="nav-links">
            <a href="principal.php">Volver al Inicio</a>
        </div>
    </nav>

    <div class="container-sm">
        <div class="panel">
            <h2 class="panel-title">Modificar Contraseña de Acceso</h2>

            <?php
            if (isset($_GET['error'])) {
                $error = $_GET['error'];
                if ($error == 'vacia') echo '<div class="alert" style="background:#f8d7da; color:#721c24;">Todos los campos son obligatorios.</div>';
                if ($error == 'coincidencia') echo '<div class="alert" style="background:#f8d7da; color:#721c24;">La nueva contraseña y la confirmación no coinciden.</div>';
                if ($error == 'incorrecta') echo '<div class="alert" style="background:#f8d7da; color:#721c24;">La contraseña actual ingresada es incorrecta.</div>';
                if ($error == 'db') echo '<div class="alert" style="background:#f8d7da; color:#721c24;">Ocurrió un problema técnico al actualizar los datos.</div>';
            }
            if (isset($_GET['status']) && $_GET['status'] == 'success') {
                echo '<div class="alert alert-success">¡Contraseña cambiada exitosamente! Usa tu nueva clave el próximo inicio de sesión.</div>';
            }
            ?>

            <form action="p_cambiar_password.php" method="POST">
                <div class="form-grid">
                    
                    <div class="form-group full-width">
                        <label>Contraseña Actual</label>
                        <input type="password" name="pass_actual" required>
                    </div>

                    <div class="form-group">
                        <label>Nueva Contraseña</label>
                        <input type="password" name="pass_nueva" required minlength="4">
                    </div>

                    <div class="form-group">
                        <label>Confirmar Nueva Contraseña</label>
                        <input type="password" name="pass_confirmar" required minlength="4">
                    </div>

                </div>
                
                <button type="submit" class="btn-submit">ACTUALIZAR CONTRASEÑA</button>
            </form>
        </div>
    </div>
</body>
</html>
</file>

<file path="cerrar_session.php">
<?php
session_start();
session_destroy();
header('location:index.php');
?>
</file>

<file path="conexion.php">
<?php

$cn=mysqli_connect("localhost","root","","db_mesadepartes_unjfsc");

?>
</file>

<file path="css/dashboard.css">
:root {
    --azul-institucional: #0a4275;
    --azul-oscuro: #062847;
    --dorado-arena: #c5a059;
    --blanco-transparente: rgba(255, 255, 255, 0.95);
    --fondo-gris: #f4f6f9;
}

* { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Montserrat', sans-serif; }

/* FONDO Y CAPA OSCURA */
body { 
    background-image: url('../img/img_principal.png'); /* Asegúrate de que la ruta sea correcta */
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    color: #333; 
    min-height: 100vh;
}
body::before {
    content: ''; position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(6, 40, 71, 0.75); z-index: -1;
}

/* NAVBAR */
.navbar {
    background-color: var(--azul-oscuro); padding: 0 40px; height: 70px; display: flex;
    justify-content: space-between; align-items: center; border-bottom: 3px solid var(--dorado-arena);
}
.navbar-brand { font-family: 'Playfair Display', serif; font-weight: 600; font-size: 1.4rem; color: white; letter-spacing: 1px;}
.navbar-brand span { color: var(--dorado-arena); }
.nav-links { display: flex; height: 100%; }
.nav-links a { color: white; text-decoration: none; font-size: 0.95rem; font-weight: 500; padding: 0 20px; display: flex; align-items: center; transition: all 0.3s; border-bottom: 3px solid transparent;}
.nav-links a:hover, .nav-links a.active { color: var(--dorado-arena); border-bottom: 3px solid var(--dorado-arena); background-color: rgba(255,255,255,0.05);}
.btn-logout { background-color: #dc3545; color: white !important; border-radius: 4px; height: 40px !important; margin-top: 15px; margin-left: 20px; padding: 0 20px !important; border-bottom: none !important;}
.btn-logout:hover { background-color: #c82333; }

/* CONTENEDORES Y PANELES */
.container { max-width: 1200px; margin: 40px auto; padding: 0 20px; }
.container-sm { max-width: 900px; margin: 40px auto; padding: 0 20px; } /* Para el perfil */
.panel { background: var(--blanco-transparente); padding: 30px; border-radius: 12px; box-shadow: 0 10px 30px rgba(0,0,0,0.15); backdrop-filter: blur(10px); }
.panel-title { color: var(--azul-institucional); font-family: 'Playfair Display', serif; font-size: 1.5rem; margin-bottom: 20px; border-bottom: 2px solid var(--dorado-arena); padding-bottom: 10px; }

/* GRIDS Y PERFIL */
.dashboard-grid { display: grid; grid-template-columns: 1fr 2fr; gap: 25px; margin-top: 25px; }
.profile-mini { display: flex; align-items: center; gap: 20px; margin-bottom: 30px; }
.profile-photo { width: 80px; height: 80px; background-color: #e9ecef; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 2.5rem; color: #adb5bd; border: 3px solid var(--dorado-arena); }
.profile-info h3 { color: var(--azul-oscuro); font-size: 1.1rem;}
.profile-info p { color: #555; font-size: 0.9rem; margin-top: 3px;}
.badge { background-color: var(--dorado-arena); color: white; padding: 4px 12px; border-radius: 15px; font-size: 0.75rem; font-weight: 600; display: inline-block; margin-top: 8px; text-transform: uppercase;}

/* BOTONES */
.action-btn { display: block; width: 100%; text-align: center; padding: 15px; margin-bottom: 15px; border-radius: 8px; text-decoration: none; font-weight: 600; transition: transform 0.2s; }
.action-btn:hover { transform: translateY(-3px); }
.btn-primary { background-color: var(--azul-institucional); color: white; }
.btn-secondary { background-color: white; color: var(--azul-institucional); border: 2px solid var(--azul-institucional); }
.btn-submit { background-color: var(--azul-institucional); color: white; border: none; padding: 15px; border-radius: 6px; font-size: 1rem; font-weight: 600; cursor: pointer; transition: background 0.3s; margin-top: 20px; width: 100%; }
.btn-submit:hover { background-color: var(--dorado-arena); }

/* ESTADÍSTICAS Y TABLAS */
.stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 15px; margin-bottom: 25px; }
.stat-card { background: white; padding: 20px; border-radius: 8px; text-align: center; border-left: 4px solid var(--dorado-arena); box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
.stat-card h2 { color: var(--azul-oscuro); font-size: 2rem; margin-bottom: 5px; }
.stat-card p { color: #666; font-size: 0.85rem; font-weight: 500; text-transform: uppercase; }
.table-container { overflow-x: auto; }
table { width: 100%; border-collapse: collapse; margin-top: 10px; }
th, td { padding: 12px 15px; text-align: left; border-bottom: 1px solid #eee; font-size: 0.9rem; }
th { background-color: var(--fondo-gris); color: var(--azul-oscuro); font-weight: 600; }
tr:hover { background-color: #f9f9f9; }
.status-badge { padding: 4px 8px; border-radius: 4px; font-size: 0.8rem; font-weight: 600; }
.status-proceso { background: #cce5ff; color: #004085; }
.status-atendido { background: #d4edda; color: #155724; }

/* FORMULARIOS (Para Perfil y Nuevo Trámite) */
.form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.form-group { display: flex; flex-direction: column; }
.form-group.full-width { grid-column: span 2; }
.form-group label { font-size: 0.85rem; font-weight: 600; color: var(--azul-oscuro); margin-bottom: 8px; text-transform: uppercase; }
.form-group input, .form-group select, .form-group textarea { padding: 12px; border: 1px solid #ccc; border-radius: 6px; font-size: 1rem; transition: border 0.3s; background: white; font-family: 'Montserrat', sans-serif;}
.form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--dorado-arena); outline: none; }
input[readonly] { background-color: #e9ecef; color: #6c757d; border: 1px dashed #adb5bd; cursor: not-allowed; }
.alert { padding: 15px; border-radius: 6px; margin-bottom: 20px; font-weight: 500; }
.alert-success { background-color: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
.helper-text { font-size: 0.8rem; color: #dc3545; margin-top: 5px; font-style: italic; }

/* =========================================
   MENÚ DESPLEGABLE (DROPDOWNS) CORREGIDO
========================================= */


.dropdown {
    position: relative;
    display: flex; /* Cambiado a flex para alineación perfecta */
    align-items: center;
    height: 100%;
}

.dropbtn {
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 5px;
    padding: 10px 15px; /* Mismo relleno que tus enlaces <a> normales */
    color: #e0e0e0; /* O el color que usen tus enlaces inactivos */
    font-weight: 500;
}
.dropdown-content {
    display: none;
    position: absolute;
    background-color: var(--azul-oscuro);
    min-width: 220px;
    box-shadow: 0px 8px 16px rgba(0,0,0,0.4);
    z-index: 10;
    top: 70px; /* Misma altura del navbar */
    border-top: 3px solid var(--dorado-arena);
    border-radius: 0 0 8px 8px;
    overflow: hidden;
}
.dropdown-content a {
    color: white;
    padding: 15px 20px;
    text-decoration: none;
    display: block;
    border-bottom: 1px solid rgba(255,255,255,0.05);
    height: auto;
}
.dropdown-content a:hover {
    background-color: rgba(255,255,255,0.1);
    color: var(--dorado-arena);
    border-bottom: 1px solid rgba(255,255,255,0.05);
}
.dropdown:hover .dropdown-content {
    display: block;
}
</file>

<file path="css/estilos.css">
:root {
    --navy-blue: #0b1d35;
    --gold-accent: #c5a059;
    --light-cream: #f9f8f4;
    --text-dark: #2c3e50;
}

/* 1. RESET DE CAJAS: El truco mágico para que nada se deforme ni se salga de su contenedor */
*,
*::before,
*::after {
    box-sizing: border-box;
}

/* 2. ESTILOS GENERALES (Fondo y centrado) */
body,
html {
    height: 100%;
    width:100%;
    margin: 0;
    font-family: 'Montserrat', sans-serif;
    background-image: url('../img/fgeneral.png');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    display: flex;
    justify-content: center;
    align-items: center;
}

.overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(11, 29, 53, 0.65);
    z-index: 1;
}

/* 3. TARJETA BASE (Usada en el Login) */
.login-card {
    background: var(--light-cream);
    padding: 3rem 4rem;
    border-radius: 4px;
    box-shadow: 0 15px 35px rgba(0, 0, 0, 0.4);
    width: 100%;
    max-width: 400px;
    /* Ancho para el login */
    z-index: 2;
    position: relative;
    border-top: 5px solid var(--gold-accent);
}

/* 4. CABECERA Y TEXTOS */
.login-header {
    text-align: center;
    margin-bottom: 2.5rem;
}

.login-header h1 {
    font-family: 'Playfair Display', serif;
    color: var(--navy-blue);
    font-size: 2rem;
    margin: 0;
    font-weight: 600;
}

.login-header p {
    color: #666;
    font-size: 0.9rem;
    margin-top: 0.5rem;
    font-style: italic;
}

/* 5. FORMULARIOS Y BOTONES */
.form-group {
    margin-bottom: 1.5rem;
}

.form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: var(--navy-blue);
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.5px;
    text-transform: uppercase;
}

.form-group input,
select {
    width: 100%;
    padding: 0.75rem 0;
    border: none;
    border-bottom: 1px solid #ccc;
    background: transparent;
    font-family: 'Montserrat', sans-serif;
    font-size: 1rem;
    color: var(--text-dark);
    transition: all 0.3s ease;
    outline: none;
    /* Aplicado a ambos */
}

.form-group input:focus,
select:focus {
    border-bottom: 2px solid var(--gold-accent);
}

.btn-submit {
    width: 100%;
    padding: 1rem;
    background-color: var(--navy-blue);
    color: var(--gold-accent);
    border: none;
    font-family: 'Montserrat', sans-serif;
    font-size: 1rem;
    font-weight: 500;
    letter-spacing: 1px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    margin-top: 1rem;
}

.btn-submit:hover {
    background-color: #071222;
}

.register-link {
    text-align: center;
    margin-top: 2rem;
    font-size: 0.9rem;
}

.register-link a {
    color: var(--navy-blue);
    text-decoration: none;
    font-weight: 500;
    border-bottom: 1px solid var(--gold-accent);
    padding-bottom: 2px;
    transition: color 0.3s ease;
}

.register-link a:hover {
    color: var(--gold-accent);
}

/* 6. ESTILOS EXTRA SOLO PARA EL REGISTRO (Sobrescribe login-card) */
.registro-card {
    max-width: 750px;
    /* Ancho expandido para grid */
    padding: 2.5rem 3.5rem;
    max-height: 90vh;
    overflow-y: auto;
}

.form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
}

.form-group.full-width {
    grid-column: span 2;
}

.seccion-dinamica {
    display: none;
    grid-column: span 2;
    background-color: rgba(197, 160, 89, 0.05);
    padding: 20px;
    border-left: 3px solid var(--gold-accent);
    margin-top: 10px;
    border-radius: 4px;
}

.seccion-titulo {
    grid-column: span 2;
    margin-bottom: 10px;
    color: var(--navy-blue);
    border-bottom: 1px solid rgba(197, 160, 89, 0.3);
    padding-bottom: 5px;
}

.alerta-error {
    background-color: #f8d7da;
    color: #721c24;
    padding: 10px;
    border-radius: 4px;
    margin-bottom: 15px;
    text-align: center;
    font-size: 0.9rem;
    border: 1px solid #f5c6cb;
}
</file>

<file path="get_distritos.php">
<?php
require 'conexion.php';

if (isset($_POST['province_id'])) {
    $prov_id = $_POST['province_id'];

    // Usamos $cn (tu variable de conexión) en lugar de $pdo
    $query = "SELECT id, name FROM ubigeo_peru_districts WHERE province_id = '$prov_id' ORDER BY name ASC";
    $result = $cn->query($query);

    echo '<option value="" disabled selected>-- Seleccione Distrito --</option>';
    
    if ($result) {
        while ($dist = $result->fetch_assoc()) {
            echo '<option value="' . $dist['id'] . '">' . $dist['name'] . '</option>';
        }
    }
}
?>
</file>

<file path="get_provincias.php">
<?php
require 'conexion.php';

if (isset($_POST['department_id'])) {
    $dep_id = $_POST['department_id'];

    // Usamos $cn (tu variable de conexión) en lugar de $pdo
    $query = "SELECT id, name FROM ubigeo_peru_provinces WHERE department_id = '$dep_id' ORDER BY name ASC";
    $result = $cn->query($query);

    echo '<option value="" disabled selected>-- Seleccione Provincia --</option>';
    
    if ($result) {
        while ($prov = $result->fetch_assoc()) {
            echo '<option value="' . $prov['id'] . '">' . $prov['name'] . '</option>';
        }
    }
}
?>
</file>

<file path="p_cambiar_password.php">
<?php
session_start();

if (!isset($_SESSION["auth"]) || $_SESSION["auth"] != "1" || $_SERVER["REQUEST_METHOD"] != "POST") {
    header("Location: index.php");
    exit();
}

require 'conexion.php'; 

$id_usuario = $_SESSION["id_usuario"];

// Capturamos y sanitizamos las claves
$pass_actual    = mysqli_real_escape_string($cn, $_POST['pass_actual']);
$pass_nueva     = mysqli_real_escape_string($cn, $_POST['pass_nueva']);
$pass_confirmar = mysqli_real_escape_string($cn, $_POST['pass_confirmar']);

// Validamos que los campos no vengan vacíos
if(empty($pass_actual) || empty($pass_nueva) || empty($pass_confirmar)) {
    header("Location: cambiar_password.php?error=vacia");
    exit();
}

// Validamos coincidencia
if($pass_nueva !== $pass_confirmar) {
    header("Location: cambiar_password.php?error=coincidencia");
    exit();
}


// 1. Verificamos que la contraseña actual sea la correcta en la BD
$query_verificar = "SELECT password FROM usuarios WHERE id_usuario = '$id_usuario'";
$resultado_verificar = mysqli_query($cn, $query_verificar);
$fila = mysqli_fetch_assoc($resultado_verificar);

if($fila['password'] !== $pass_actual) {
    header("Location: cambiar_password.php?error=incorrecta");
    exit();
}

// 2. Realizamos la actualización
$query_update = "UPDATE usuarios SET password = '$pass_nueva' WHERE id_usuario = '$id_usuario'";

if(mysqli_query($cn, $query_update)) {
    header("Location: cambiar_password.php?status=success");
    exit();
} else {
    header("Location: cambiar_password.php?error=db");
    exit();
}
?>
</file>

<file path="p_login_mesa.php">
<?php
session_start();
require 'conexion.php'; 

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    
    $identificador = $_POST['identificador'];
    $password = $_POST['password'];

    // Lógica del correo (Si inician sesión sin el @)
    $dominio = "@unjfsc.edu.pe"; 
    if (strpos($identificador, '@') === false) {
        $correo_generado = $identificador . $dominio;
    } else {
        $correo_generado = $identificador;
    }

    $query = "SELECT * FROM usuarios WHERE correo = '$correo_generado'";
    $resultado = mysqli_query($cn, $query);

    if (mysqli_num_rows($resultado) == 1) {
        $usuario = mysqli_fetch_assoc($resultado);
        
        // -------------------------------------------------------------
        // VALIDACIÓN ESTRICTA: Solo entran Administradores/Mesa de partes
        // OJO: Cambia el "5" por el id_tipo real de tu base de datos 
        // que corresponda al personal de Mesa de Partes.
        // -------------------------------------------------------------
        if ($usuario['id_tipo'] != 5) {
            header("Location: login_mesa.php?error=user");
            exit();
        }
        
        // Verificamos contraseña
        if ($password === $usuario['password']) {
            
            // Credenciales correctas
            $_SESSION["auth_mesa"] = "1"; // Variable de sesión exclusiva
            $_SESSION["id_usuario"] = $usuario['id_usuario'];
            $_SESSION["id_tipo"] = $usuario['id_tipo']; 
            
            // Lo enviamos a la bandeja de administración
            header("Location: principal_mesa.php"); 
            exit();
        } else {
            // Contraseña incorrecta
            header("Location: login_mesa.php?error=pass");
            exit();
        }
    } else {
        // Usuario no encontrado
        header("Location: login_mesa.php?error=user");
        exit();
    }
} else {
    header("Location: login_mesa.php");
    exit();
}
?>
</file>

<file path="p_login.php">
<?php
session_start();
require 'conexion.php'; // Llamamos a tu conexión

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    
    // 1. Recibimos los datos del formulario (AHORA INCLUIMOS EL TIPO)
    $identificador = $_POST['identificador'];
    $password = $_POST['password'];
    $tipo_login = $_POST['tipo_login']; // <-- El name del <select> de tu index

    // 2. Lógica "Por debajo de la mesa" para el correo
    $dominio = "@unjfsc.edu.pe"; 
    
    if (strpos($identificador, '@') === false) {
        $correo_generado = $identificador . $dominio;
    } else {
        $correo_generado = $identificador;
    }

    // 3. Buscamos al usuario en la BD
    $query = "SELECT * FROM usuarios WHERE correo = '$correo_generado'";
    $resultado = mysqli_query($cn, $query);

    if (mysqli_num_rows($resultado) == 1) {
        $usuario = mysqli_fetch_assoc($resultado);
        
        // 4. NUEVA VALIDACIÓN: ¿El tipo que eligió coincide con su tipo real en la BD?
        if ($tipo_login != $usuario['id_tipo']) {
            // Si eligió "Alumno" pero en la BD es "Docente", lo rebotamos
            header("Location: index.php?error=tipo");
            exit();
        }
        
        // 5. Verificamos si la contraseña coincide (SIN encriptar, según tu código actual)
        if ($password === $usuario['password']) {
            
            // Credenciales y tipo correctos
            $_SESSION["auth"] = "1";
            $_SESSION["id_usuario"] = $usuario['id_usuario'];
            $_SESSION["id_tipo"] = $usuario['id_tipo']; 
            
            // Lo enviamos a la plataforma principal
            header("Location: principal.php");
            exit();
        } else {
            // Contraseña incorrecta
            header("Location: index.php?error=pass");
            exit();
        }
    } else {
        // Usuario no encontrado
        header("Location: index.php?error=user");
        exit();
    }
} else {
    // Si alguien intenta entrar por URL directa
    header("Location: index.php");
    exit();
}
?>
</file>

<file path="p_perfil_foto.php">
<?php
session_start();

if (!isset($_SESSION["auth"]) || $_SESSION["auth"] != "1" || $_SERVER["REQUEST_METHOD"] != "POST") {
    header("Location: index.php");
    exit();
}

$id_usuario = $_SESSION["id_usuario"];

if (isset($_FILES['foto']) && $_FILES['foto']['error'] == 0) {
    $archivo = $_FILES['foto'];
    $nombre_original = $archivo['name'];
    $tipo_archivo = $archivo['type'];
    $temporal = $archivo['tmp_name'];
    $tamano = $archivo['size'];
    
    // 1. Validar la extensión del archivo
    $ext = strtolower(pathinfo($nombre_original, PATHINFO_EXTENSION));
    $extensiones_permitidas = ['jpg', 'jpeg', 'png'];
    
    if (!in_array($ext, $extensiones_permitidas)) {
        header("Location: perfil_foto.php?error=formato");
        exit();
    }
    
    // 2. Validar tamaño (Máximo 2 Megabytes = 2 * 1024 * 1024 bytes)
    if ($tamano > 2097152) {
        header("Location: perfil_foto.php?error=tamano");
        exit();
    }
    
    // 3. Crear la carpeta "fotos" si no existe en el servidor
    $carpeta_destino = "fotos/";
    if (!file_exists($carpeta_destino)) {
        mkdir($carpeta_destino, 0777, true);
    }
    
    // 4. Limpiar cualquier foto anterior con otra extensión para que no deje residuos
    foreach ($extensiones_permitidas as $e) {
        $foto_vieja = $carpeta_destino . "usuario_" . $id_usuario . "." . $e;
        if (file_exists($foto_vieja)) {
            unlink($foto_vieja);
        }
    }
    
    // 5. Definir la nueva ruta única
    $nuevo_nombre = "usuario_" . $id_usuario . "." . $ext;
    $ruta_final = $carpeta_destino . $nuevo_nombre;
    
    // 6. Mover el archivo desde la ubicación temporal del servidor a la carpeta final
    if (move_uploaded_file($temporal, $ruta_final)) {
        header("Location: perfil_foto.php?status=success");
        exit();
    } else {
        header("Location: perfil_foto.php?error=subida");
        exit();
    }
} else {
    header("Location: perfil_foto.php?error=subida");
    exit();
}
?>
</file>

<file path="p_perfil.php">
<?php
session_start();

if (!isset($_SESSION["auth"]) || $_SESSION["auth"] != "1" || $_SERVER["REQUEST_METHOD"] != "POST") {
    header("Location: index.php");
    exit();
}

require 'conexion.php'; 

$id_usuario = $_SESSION["id_usuario"];
$id_tipo = $_SESSION["id_tipo"];

if ($id_tipo == 4) {
    // -----------------------------------------------------
    // ACTUALIZAR DATOS JURÍDICOS (Institución)
    // -----------------------------------------------------
    
    // Recibimos y limpiamos los datos editables
    $correo_empresarial = mysqli_real_escape_string($cn, $_POST['correo_empresarial']);
    $dir_empresa = mysqli_real_escape_string($cn, $_POST['dir_empresa']);

    $query = "UPDATE datos_juridicos 
              SET correo_empresarial = '$correo_empresarial', 
                  dir_empresa = '$dir_empresa' 
              WHERE id_usuario = '$id_usuario'";
              
    if(mysqli_query($cn, $query)){
        // Éxito, redirigimos al perfil con mensaje
        header("Location: perfil.php?status=success");
        exit();
    } else {
        echo "Error al actualizar la base de datos: " . mysqli_error($cn);
    }

} else {
    // -----------------------------------------------------
    // ACTUALIZAR DATOS PERSONALES (Alumno, Docente, Egresado)
    // -----------------------------------------------------
    
    // Recibimos y limpiamos los datos editables
    $celular = mysqli_real_escape_string($cn, $_POST['celular']);
    $telefono_fijo = mysqli_real_escape_string($cn, $_POST['telefono_fijo']);
    $correo_personal = mysqli_real_escape_string($cn, $_POST['correo_personal']);
    $direccion_texto = mysqli_real_escape_string($cn, $_POST['direccion_texto']);
    $referencia_direccion = mysqli_real_escape_string($cn, $_POST['referencia_direccion']);

    $query = "UPDATE datos_personales 
              SET celular = '$celular', 
                  telefono_fijo = '$telefono_fijo', 
                  correo_personal = '$correo_personal', 
                  direccion_texto = '$direccion_texto',
                  referencia_direccion = '$referencia_direccion'
              WHERE id_usuario = '$id_usuario'";
              
    if(mysqli_query($cn, $query)){
        // Éxito, redirigimos al perfil con mensaje
        header("Location: perfil.php?status=success");
        exit();
    } else {
        echo "Error al actualizar la base de datos: " . mysqli_error($cn);
    }
}
?>
</file>

<file path="p_registro.php">
<?php
// Llamamos a tu archivo de conexión
require 'conexion.php'; 

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    
    // 1. Recibir los datos generales
    $id_tipo = $_POST['tipo_usuario'];
    $correo_contacto = $_POST['correo']; // El correo personal/empresarial del form
    $password = $_POST['password']; // SIN ENCRIPTAR, texto plano

    // 2. LÓGICA PARA CREAR EL CORREO DEL SISTEMA
    // Identificamos de dónde sacar el dato según el tipo de usuario
    $identificador = "";
    
    if ($id_tipo == '1') {
        $identificador = $_POST['cod_universitario'];
    } elseif ($id_tipo == '2') {
        $identificador = $_POST['cod_admin'];
    } elseif ($id_tipo == '3') {
        $identificador = $_POST['cod_egresado'];
    } elseif ($id_tipo == '4') {
        $identificador = $_POST['ruc'];
    }

    // Armamos el correo oficial para que inicie sesión
    $correo_sistema = $identificador . "@unjfsc.edu.pe";

    // Iniciar transacción
    $cn->begin_transaction();

    try {
        // PASO A: Insertar en la tabla principal 'usuarios'
        $sql_usuario = "INSERT INTO usuarios (correo, password, id_tipo) VALUES ('$correo_sistema', '$password', '$id_tipo')";
        $cn->query($sql_usuario);
        
        // Obtener el ID del usuario que se acaba de crear
        $id_nuevo_usuario = $cn->insert_id;

    // PASO B: Guardar Datos Personales (Para Alumnos, Personal y Egresados)
        if ($id_tipo == '1' || $id_tipo == '2' || $id_tipo == '3') {
            
            $nombres = $_POST['nombres'];
            $ape_paterno = $_POST['ape_paterno'];
            $ape_materno = $_POST['ape_materno'];
            $tipo_doc = $_POST['tipo_doc'];
            $num_doc = $_POST['num_doc'];
            $celular = $_POST['celular'];
            $telefono_fijo = $_POST['telefono_fijo'];
            
            // Recibimos la dirección y SOLO el ID del distrito (La llave foránea)
            $direccion_texto = $_POST['tipo_via'] . ' ' . $_POST['nombre_via'];
            $id_distrito = $_POST['id_distrito']; 
            $referencia = $_POST['referencia'];

            // Insertamos usando id_distrito en lugar de los textos sueltos
            $sql_personal = "INSERT INTO datos_personales 
                            (id_usuario, nombres, apellido_paterno, apellido_materno, tipo_documento, numero_documento, celular, telefono_fijo, correo_personal, direccion_texto, id_distrito, referencia_direccion) 
                            VALUES 
                            ('$id_nuevo_usuario', '$nombres', '$ape_paterno', '$ape_materno', '$tipo_doc', '$num_doc', '$celular', '$telefono_fijo', '$correo_contacto', '$direccion_texto', '$id_distrito', '$referencia')";
            $cn->query($sql_personal);
        }

        // PASO C: Guardar los datos específicos según el perfil
        if ($id_tipo == '1') { // ALUMNO
            
            $anio_ingreso = $_POST['anio_ingreso'];
            $ciclo = $_POST['ciclo'];
            $id_escuela = $_POST['id_escuela']; 

            $sql_alumno = "INSERT INTO datos_alumnos (id_usuario, codigo_universitario, id_escuela, año_ingreso, ciclo_actual) 
                           VALUES ('$id_nuevo_usuario', '$identificador', '$id_escuela', '$anio_ingreso', '$ciclo')";
            $cn->query($sql_alumno);

        } 
        elseif ($id_tipo == '2') { // PERSONAL / DOCENTE
            
            $cargo = $_POST['cargo'];
            $condicion = $_POST['condicion'];
            $categoria = $_POST['categoria'];

            $sql_personal_lab = "INSERT INTO datos_personal (id_usuario, codigo_administrativo, cargo, condicion_laboral, nivel_categoria) 
                                 VALUES ('$id_nuevo_usuario', '$identificador', '$cargo', '$condicion', '$categoria')";
            $cn->query($sql_personal_lab);

        } 
        elseif ($id_tipo == '3') { // EGRESADO
            
            $anio_ingreso = $_POST['anio_ingreso_egreso'];
            $anio_egreso = $_POST['anio_egreso'];
            $id_escuela_egre = $_POST['id_escuela_egresado']; 

            $sql_egresado = "INSERT INTO datos_egresados (id_usuario, codigo_universitario, id_escuela, año_ingreso, año_egreso) 
                             VALUES ('$id_nuevo_usuario', '$identificador', '$id_escuela_egre', '$anio_ingreso', '$anio_egreso')";
            $cn->query($sql_egresado);

        } 
        elseif ($id_tipo == '4') { // INSTITUCIÓN EXTERNA
            
            $razon_social = $_POST['razon_social'];
            $direccion_inst = $_POST['direccion_inst'];
            $id_distrito_inst = $_POST['id_distrito']; // También recibe el ID de distrito para la empresa

            // Actualizado para incluir id_distrito
            $sql_institucion = "INSERT INTO datos_juridicos (id_usuario, ruc, razon_social, dir_empresa, id_distrito, correo_empresarial) 
                                VALUES ('$id_nuevo_usuario', '$identificador', '$razon_social', '$direccion_inst', '$id_distrito_inst', '$correo_contacto')";
            $cn->query($sql_institucion);
            
        }

        // Confirmamos todos los INSERTS
        $cn->commit();
        
        // Redirigimos avisándole cuál es su usuario
        echo "<script>
                alert('Registro exitoso. Para iniciar sesión usa tu identificador: $identificador');
                window.location.href = 'index.php';
              </script>";

    } catch (Exception $e) {
        $cn->rollback();
        echo "Error en la Base de Datos: " . $e->getMessage();
    }
}

$cn->close();
?>
</file>

<file path="perfil_foto.php">
<?php
session_start();

if (!isset($_SESSION["auth"]) || $_SESSION["auth"] != "1") {
    header("Location: index.php");
    exit();
}

require 'conexion.php'; 

$id_usuario = $_SESSION["id_usuario"];

// Verificamos si ya existe una foto para este usuario
// Formatos admitidos en orden de prioridad
$formatos = ['jpg', 'jpeg', 'png'];
$foto_perfil = "";

foreach ($formatos as $ext) {
    $ruta_posible = "fotos/usuario_" . $id_usuario . "." . $ext;
    if (file_exists($ruta_posible)) {
        $foto_perfil = $ruta_posible;
        break;
    }
}
?>

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cambiar Foto de Perfil | UNJFSC</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/dashboard.css">
    <style>
        /* Ajuste específico para centrar la visualización de la foto */
        .preview-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 25px;
        }
        .profile-preview {
            width: 160px;
            height: 160px;
            border-radius: 50%;
            border: 4px solid var(--dorado-arena);
            object-fit: cover;
            box-shadow: 0 5px 15px rgba(0,0,0,0.15);
            background-color: #f8f9fa;
        }
        .profile-icon-placeholder {
            width: 160px;
            height: 160px;
            border-radius: 50%;
            border: 4px dashed var(--dorado-arena);
            background-color: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 5rem;
            color: #adb5bd;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-brand">UNJFSC <span>| Perfil</span></div>
        <div class="nav-links">
            <a href="principal.php">Volver al Inicio</a>
        </div>
    </nav>

    <div class="container-sm">
        <div class="panel">
            <h2 class="panel-title">Actualizar Foto de Perfil</h2>

            <?php
            if (isset($_GET['error'])) {
                $error = $_GET['error'];
                if ($error == 'formato') echo '<div class="alert" style="background:#f8d7da; color:#721c24;">Solo se permiten formatos JPG, JPEG o PNG.</div>';
                if ($error == 'tamano') echo '<div class="alert" style="background:#f8d7da; color:#721c24;">La imagen excede el tamaño máximo permitido (2MB).</div>';
                if ($error == 'subida') echo '<div class="alert" style="background:#f8d7da; color:#721c24;">Hubo un problema al guardar el archivo. Inténtalo de nuevo.</div>';
            }
            if (isset($_GET['status']) && $_GET['status'] == 'success') {
                echo '<div class="alert alert-success">¡Tu foto de perfil ha sido actualizada con éxito!</div>';
            }
            ?>

            <div class="preview-container">
                <?php if (!empty($foto_perfil)): ?>
                    <img src="<?php echo $foto_perfil . '?v=' . time(); ?>" alt="Foto de perfil" class="profile-preview">
                <?php else: ?>
                    <div class="profile-icon-placeholder">&#128100;</div>
                <?php endif; ?>
                <p style="margin-top: 10px; font-size: 0.9rem; color: #666;">Vista previa actual</p>
            </div>

            <form action="p_perfil_foto.php" method="POST" enctype="multipart/form-data">
                <div class="form-group full-width">
                    <label>Selecciona una nueva imagen</label>
                    <input type="file" name="foto" accept="image/jpeg, image/jpg, image/png" required>
                    <span class="helper-text">* Formatos aceptados: JPG, JPEG, PNG. Tamaño máximo recomendado: 2MB.</span>
                </div>
                
                <button type="submit" class="btn-submit">SUBIR E INSTALAR FOTO</button>
            </form>
        </div>
    </div>
</body>
</html>
</file>

<file path="perfil.php">
<?php
session_start();

if (!isset($_SESSION["auth"]) || $_SESSION["auth"] != "1") {
    header("Location: index.php");
    exit();
}

require 'conexion.php'; 

$id_usuario = $_SESSION["id_usuario"];
$id_tipo = $_SESSION["id_tipo"];

// Variables para almacenar los datos (se llenarán según el tipo)
$datos = [];

if ($id_tipo == 4) { // INSTITUCIÓN
    $query = "SELECT u.correo, j.ruc, j.razon_social, j.correo_empresarial, j.dir_empresa 
              FROM usuarios u 
              INNER JOIN datos_juridicos j ON u.id_usuario = j.id_usuario 
              WHERE u.id_usuario = '$id_usuario'";
    $resultado = mysqli_query($cn, $query);
    $datos = mysqli_fetch_assoc($resultado);
} else { // ALUMNO, DOCENTE O EGRESADO
    $query = "SELECT u.correo, p.numero_documento, p.nombres, p.apellido_paterno, p.apellido_materno, 
                     p.correo_personal, p.direccion_texto, p.referencia_direccion, p.telefono_fijo, p.celular 
              FROM usuarios u 
              INNER JOIN datos_personales p ON u.id_usuario = p.id_usuario 
              WHERE u.id_usuario = '$id_usuario'";
    $resultado = mysqli_query($cn, $query);
    $datos = mysqli_fetch_assoc($resultado);
}
?>

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil | UNJFSC</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/dashboard.css">
</head>
<body>
    <nav class="navbar">
        <div class="navbar-brand">UNJFSC <span>| Mesa de Partes</span></div>
        <div class="nav-links">
            <a href="principal.php">Volver al Inicio</a>
        </div>
    </nav>

    <div class="container">
        <div class="panel">
            <h2 class="panel-title">Actualizar Mis Datos</h2>

            <?php
            if (isset($_GET['status']) && $_GET['status'] == 'success') {
                echo '<div class="alert alert-success">¡Tus datos han sido actualizados correctamente!</div>';
            }
            ?>

            <form action="p_perfil.php" method="POST">
                <div class="form-grid">
                    
                    <div class="form-group full-width">
                        <label>Correo Institucional / Login</label>
                        <input type="email" value="<?php echo $datos['correo']; ?>" readonly>
                        <span class="helper-text">* El correo de inicio de sesión no puede ser modificado.</span>
                    </div>

                    <?php if ($id_tipo == 4): ?>
                        <div class="form-group">
                            <label>RUC</label>
                            <input type="text" value="<?php echo $datos['ruc']; ?>" readonly>
                        </div>
                        <div class="form-group">
                            <label>Razón Social</label>
                            <input type="text" value="<?php echo $datos['razon_social']; ?>" readonly>
                        </div>

                        <div class="form-group full-width" style="margin-top: 15px; border-top: 1px solid #eee; padding-top: 15px;">
                            <h3 style="color: var(--dorado-arena); margin-bottom: 10px;">Datos de Contacto (Editables)</h3>
                        </div>

                        <div class="form-group">
                            <label>Correo de Contacto Empresarial</label>
                            <input type="email" name="correo_empresarial" value="<?php echo $datos['correo_empresarial']; ?>" required>
                        </div>
                        <div class="form-group">
                            <label>Dirección Fiscal</label>
                            <input type="text" name="dir_empresa" value="<?php echo $datos['dir_empresa']; ?>" required>
                        </div>

                    <?php else: ?>
                        <div class="form-group">
                            <label>DNI / Documento</label>
                            <input type="text" value="<?php echo $datos['numero_documento']; ?>" readonly>
                        </div>
                        <div class="form-group">
                            <label>Nombres</label>
                            <input type="text" value="<?php echo $datos['nombres']; ?>" readonly>
                        </div>
                        <div class="form-group">
                            <label>Apellido Paterno</label>
                            <input type="text" value="<?php echo $datos['apellido_paterno']; ?>" readonly>
                        </div>
                        <div class="form-group">
                            <label>Apellido Materno</label>
                            <input type="text" value="<?php echo $datos['apellido_materno']; ?>" readonly>
                        </div>

                        <div class="form-group full-width" style="margin-top: 15px; border-top: 1px solid #eee; padding-top: 15px;">
                            <h3 style="color: var(--dorado-arena); margin-bottom: 10px;">Datos de Contacto (Editables)</h3>
                        </div>

                        <div class="form-group">
                            <label>Celular</label>
                            <input type="text" name="celular" value="<?php echo $datos['celular']; ?>" required maxlength="15">
                        </div>
                        <div class="form-group">
                            <label>Teléfono Fijo (Opcional)</label>
                            <input type="text" name="telefono_fijo" value="<?php echo $datos['telefono_fijo']; ?>" maxlength="15">
                        </div>
                        <div class="form-group">
                            <label>Correo Personal (Opcional)</label>
                            <input type="email" name="correo_personal" value="<?php echo $datos['correo_personal']; ?>">
                        </div>
                        <div class="form-group full-width">
                            <label>Dirección de Residencia</label>
                            <input type="text" name="direccion_texto" value="<?php echo $datos['direccion_texto']; ?>" required>
                        </div>
                        <div class="form-group full-width">
                            <label>Referencia de Dirección</label>
                            <input type="text" name="referencia_direccion" value="<?php echo $datos['referencia_direccion']; ?>">
                        </div>
                    <?php endif; ?>

                </div>
                
                <button type="submit" class="btn-submit">GUARDAR CAMBIOS</button>
            </form>
        </div>
    </div>
</body>
</html>
</file>

<file path="principal_mesa.php">

</file>

<file path="principal.php">
<?php
session_start();

if (!isset($_SESSION["auth"]) || $_SESSION["auth"] != "1") {
    header("Location: index.php");
    exit();
}

require 'conexion.php';

$id_usuario = $_SESSION["id_usuario"];
$id_tipo = $_SESSION["id_tipo"];

// ----------------------------------------------------
// 1. OBTENER DATOS DEL USUARIO (Perfil)
// ----------------------------------------------------
$nombre_usuario = "";
$documento_usuario = "";
$correo_usuario = "";
$tipo_texto = "";

if ($id_tipo == 4) {
    $query = "SELECT j.razon_social, j.ruc, u.correo 
              FROM datos_juridicos j 
              INNER JOIN usuarios u ON j.id_usuario = u.id_usuario 
              WHERE u.id_usuario = '$id_usuario'";
    $resultado = mysqli_query($cn, $query);
    if ($fila = mysqli_fetch_assoc($resultado)) {
        $nombre_usuario = $fila['razon_social'];
        $documento_usuario = "RUC: " . $fila['ruc'];
        $correo_usuario = $fila['correo'];
        $tipo_texto = "Institución Externa";
    }
} else {
    $query = "SELECT p.nombres, p.apellido_paterno, p.apellido_materno, p.numero_documento, u.correo 
              FROM datos_personales p 
              INNER JOIN usuarios u ON p.id_usuario = u.id_usuario 
              WHERE u.id_usuario = '$id_usuario'";
    $resultado = mysqli_query($cn, $query);
    if ($fila = mysqli_fetch_assoc($resultado)) {
        $nombre_usuario = $fila['nombres'] . " " . $fila['apellido_paterno'] . " " . $fila['apellido_materno'];
        $documento_usuario = "DNI/Doc: " . $fila['numero_documento'];
        $correo_usuario = $fila['correo'];

        if ($id_tipo == 1)
            $tipo_texto = "Alumno";
        if ($id_tipo == 2)
            $tipo_texto = "Docente / Personal";
        if ($id_tipo == 3)
            $tipo_texto = "Egresado";
    }
}

// ----------------------------------------------------
// 2. OBTENER ESTADÍSTICAS REALES DE LA BD
// ----------------------------------------------------
$pendientes = 0;
$en_proceso = 0;
$atendidos = 0;

/* Buscamos todos los trámites del usuario y los agrupamos por su estado */
$query_stats = "SELECT e.nombre_estado, COUNT(t.id_tramite) as total 
                FROM tramites t 
                INNER JOIN estados_tramite e ON t.id_estado = e.id_estado 
                WHERE t.id_usuario = '$id_usuario' 
                GROUP BY e.id_estado";
$res_stats = mysqli_query($cn, $query_stats);

if ($res_stats) {
    while ($row = mysqli_fetch_assoc($res_stats)) {
        $estado = strtolower($row['nombre_estado']);
        // Clasificamos inteligentemente según el nombre del estado en tu BD
        if (strpos($estado, 'pendient') !== false || strpos($estado, 'ingresad') !== false) {
            $pendientes += $row['total'];
        } elseif (strpos($estado, 'proces') !== false || strpos($estado, 'derivad') !== false || strpos($estado, 'observad') !== false) {
            $en_proceso += $row['total'];
        } elseif (strpos($estado, 'atendid') !== false || strpos($estado, 'finalizad') !== false || strpos($estado, 'rechazad') !== false) {
            $atendidos += $row['total'];
        }
    }
}
?>

<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel Principal | UNJFSC</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Montserrat:wght@300;400;500;600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="css/dashboard.css">
</head>

<body>

    <nav class="navbar">
        <div class="navbar-brand">UNJFSC <span>| Mesa de Partes</span></div>
        <div class="nav-links">
            <a href="principal.php" class="active">Inicio</a>

            <div class="dropdown">
                <a class="dropbtn">Trámites ▼</a>
                <div class="dropdown-content">
                    <a href="nuevo_tramite.php">Iniciar Nuevo Trámite</a>
                    <a href="mis_tramites.php">Revisar Mis Trámites</a>
                    <a href="historial_tramites.php">Historial Completo</a>
                </div>
            </div>

            <div class="dropdown">
                <a class="dropbtn">Mi Cuenta ▼</a>
                <div class="dropdown-content">
                    <a href="perfil.php">Actualizar Datos Personales</a>
                    <a href="perfil_foto.php">Cambiar Foto de Perfil</a>
                    <a href="cambiar_password.php">Cambiar Contraseña</a>
                </div>
            </div>

            <a href="cerrar_session.php" class="btn-logout">Cerrar Sesión</a>
        </div>
    </nav>

    <div class="container">
        <div class="dashboard-grid">

            <div class="left-column">
                <div class="panel" style="margin-bottom: 25px;">
                    <div class="profile-mini">
                        <div class="profile-photo">
                            <?php
                            $formatos = ['jpg', 'jpeg', 'png'];
                            $foto_encontrada = "";
                            foreach ($formatos as $ext) {
                                if (file_exists("fotos/usuario_" . $id_usuario . "." . $ext)) {
                                    $foto_encontrada = "fotos/usuario_" . $id_usuario . "." . $ext;
                                    break;
                                }
                            }
                            if (!empty($foto_encontrada)): ?>
                                <img src="<?php echo $foto_encontrada . '?v=' . time(); ?>"
                                    style="width:100%; height:100%; border-radius:50%; object-fit:cover;">
                            <?php else: ?>
                                &#128100;
                            <?php endif; ?>
                        </div>
                        <div class="profile-info">
                            <h3><?php echo $nombre_usuario; ?></h3>
                            <p><?php echo $documento_usuario; ?></p>
                            <div class="badge"><?php echo $tipo_texto; ?></div>
                        </div>
                    </div>
                </div>

                <div class="panel">
                    <h2 class="panel-title">Acciones Rápidas</h2>
                    <a href="nuevo_tramite.php" class="action-btn btn-primary">+ Iniciar Nuevo Trámite</a>
                    <a href="mis_tramites.php" class="action-btn btn-secondary">Ver Estado de Trámites</a>
                    <a href="perfil.php" class="action-btn btn-secondary">Actualizar Datos</a>
                </div>
            </div>

            <div class="right-column">

                <div class="stats-grid">
                    <div class="stat-card" style="border-color: #ffc107;">
                        <h2><?php echo $pendientes; ?></h2>
                        <p>Pendientes</p>
                    </div>
                    <div class="stat-card" style="border-color: #0d6efd;">
                        <h2><?php echo $en_proceso; ?></h2>
                        <p>En Proceso</p>
                    </div>
                    <div class="stat-card" style="border-color: #198754;">
                        <h2><?php echo $atendidos; ?></h2>
                        <p>Atendidos</p>
                    </div>
                </div>

                <div class="panel">
                    <h2 class="panel-title">Mis Trámites Recientes</h2>
                    <div class="table-container">
                        <table>
                            <thead>
                                <tr>
                                    <th>ID Trámite</th>
                                    <th>Tipo de Documento</th>
                                    <th>Fecha Envío</th>
                                    <th>Estado</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php
                                // ----------------------------------------------------
// 3. OBTENER ÚLTIMOS 5 TRÁMITES REALES DE LA BD
// ----------------------------------------------------
                                $query_recientes = "SELECT t.id_tramite, t.numero_expediente, tp.nombre_tramite, t.fecha_envio, e.nombre_estado 
                                  FROM tramites t 
                    INNER JOIN tipos_tramite tp ON t.id_tipo_tramite = tp.id_tipo_tramite 
                    INNER JOIN estados_tramite e ON t.id_estado = e.id_estado 
                    WHERE t.id_usuario = '$id_usuario' 
                    ORDER BY t.fecha_envio DESC 
                    LIMIT 5";

                                $res_recientes = mysqli_query($cn, $query_recientes);

                                if ($res_recientes && mysqli_num_rows($res_recientes) > 0) {
                                    while ($tramite = mysqli_fetch_assoc($res_recientes)) {

                                        // Darle color al badge según el estado
                                        $estado_bd = strtolower($tramite['nombre_estado']);
                                        $clase_badge = "status-pendiente"; // Por defecto
                                
                                        if (strpos($estado_bd, 'proces') !== false)
                                            $clase_badge = "status-proceso";
                                        if (strpos($estado_bd, 'atendid') !== false)
                                            $clase_badge = "status-atendido";

                                        // CORRECCIÓN AQUÍ: Usamos fecha_envio
                                        $fecha_formateada = date("d/m/Y H:i", strtotime($tramite['fecha_envio']));

                                        echo "<tr>";
                                        echo "<td><strong>" . $tramite['numero_expediente'] . "</strong></td>"; // Mostramos el EXP real
                                        echo "<td>" . $tramite['nombre_tramite'] . "</td>";
                                        echo "<td>" . $fecha_formateada . "</td>";
                                        echo "<td><span class='status-badge $clase_badge'>" . $tramite['nombre_estado'] . "</span></td>";
                                        echo "</tr>";
                                    }
                                } else {
                                    // Si no hay trámites, mostramos un mensaje bonito
                                    echo "<tr>
                                            <td colspan='4' style='text-align: center; color: #888; padding: 30px;'>
                                                <div style='font-size: 2.5rem; margin-bottom: 10px;'>📂</div>
                                                <em>Aún no has enviado ningún trámite.</em><br>
                                                <a href='nuevo_tramite.php' style='color: var(--dorado-arena); font-weight:600; text-decoration:none;'>Inicia tu primer trámite aquí</a>
                                            </td>
                                          </tr>";
                                }
                                ?>
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
        </div>
    </div>

</body>

</html>
</file>

<file path="registro.php">
<?php
require 'conexion.php';

// 1. Consultar escuelas (Se queda exactamente IGUAL)
$query_escuelas = "SELECT id_escuela, nombre_escuela FROM escuelas_profesionales ORDER BY nombre_escuela ASC";
$result_escuelas = $cn->query($query_escuelas);

$escuelas = [];
if ($result_escuelas) {
    while ($row = $result_escuelas->fetch_assoc()) {
        $escuelas[] = $row;
    }
}

// 2. Consultar departamentos (CORREGIDO: Apunta a tu tabla real y sus columnas id, name)
$query_deps = "SELECT id, name FROM ubigeo_peru_departments ORDER BY name ASC";
$result_deps = $cn->query($query_deps);

$departamentos = [];
if ($result_deps) {
    while ($row = $result_deps->fetch_assoc()) {
        $departamentos[] = $row; // Guardará arreglos con las llaves 'id' y 'name'
    }
}
?>
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro | Mesa de Partes UNJFSC</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;1,400&family=Montserrat:wght@300;400;500&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="css/estilos.css">

</head>

<body>
    <div class="overlay"></div>

    <div class="login-card registro-card">
        <div class="login-header">
            <h1>Registro de Usuario</h1>
            <p>Seleccione su perfil para continuar</p>
        </div>

        <form action="p_registro.php" method="POST">

            <div class="form-group full-width">
                <label for="tipo_usuario">Tipo de Usuario *</label>
                <select id="tipo_usuario" name="tipo_usuario" required onchange="mostrarCampos()">
                    <option value="" disabled selected>-- Seleccione una opción --</option>
                    <option value="1">Alumno</option>
                    <option value="2">Personal / Docente</option>
                    <option value="3">Egresado</option>
                    <option value="4">Institución Externa</option>
                </select>
            </div>

            <div class="form-grid">
                <div class="form-group full-width">
                    <label for="correo">Correo Electrónico (Contacto Personal/Empresarial) *</label>
                    <input type="email" id="correo" name="correo" required>
                </div>
                <div class="form-group full-width">
                    <label for="password">Contraseña (Para acceder al sistema) *</label>
                    <input type="password" id="password" name="password" required>
                </div>
            </div>

            <div id="seccion_personales" class="form-grid seccion-dinamica">
                <div class="seccion-titulo"><strong>Datos Personales y de Contacto</strong></div>

                <div class="form-group full-width">
                    <label for="nombres">Nombres Completos</label>
                    <input type="text" id="nombres" name="nombres">
                </div>
                <div class="form-group">
                    <label for="ape_paterno">Apellido Paterno</label>
                    <input type="text" id="ape_paterno" name="ape_paterno">
                </div>
                <div class="form-group">
                    <label for="ape_materno">Apellido Materno</label>
                    <input type="text" id="ape_materno" name="ape_materno">
                </div>
                <div class="form-group">
                    <label for="tipo_doc">Tipo de Documento</label>
                    <select id="tipo_doc" name="tipo_doc">
                        <option value="DNI">DNI</option>
                        <option value="Carnet Universitario">Carnet de Extranjeria</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="num_doc">Número de Documento</label>
                    <input type="text" id="num_doc" name="num_doc" maxlength="15">
                </div>
                <div class="form-group">
                    <label for="celular">Celular</label>
                    <input type="text" id="celular" name="celular" maxlength="9">
                </div>
                <div class="form-group">
                    <label for="telefono_fijo">Teléfono Fijo</label>
                    <input type="text" id="telefono_fijo" name="telefono_fijo" maxlength="9">
                </div>

                <div class="seccion-titulo" style="margin-top: 10px;"><strong>Domicilio / Notificación</strong></div>
                <div class="form-group">
                    <label for="tipo_via">Tipo de Vía</label>
                    <select id="tipo_via" name="tipo_via">
                        <option value="Av.">Avenida</option>
                        <option value="Calle">Calle</option>
                        <option value="Urb.">Urbanización</option>
                        <option value="Otro">Otro</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="nombre_via">Nombre de Vía / N° / Mz / Lt</label>
                    <input type="text" id="nombre_via" name="nombre_via">
                </div>

                <div class="form-group full-width">
                    <label for="referencia">Referencia de ubicación</label>
                    <input type="text" id="referencia" name="referencia">
                </div>

            </div>
            <div id="seccion_ubicacion" class="form-grid seccion-dinamica" style="display: none;">
                <div class="seccion-titulo" style="margin-top: 10px;"><strong>Ubicación Geográfica</strong></div>

                <div class="form-group">
                    <label for="departamento">Departamento</label>
                    <select id="departamento" name="departamento" onchange="cargarProvincias(this.value)">
                        <option value="" disabled selected>-- Seleccione Departamento --</option>
                        <?php foreach ($departamentos as $dep) { ?>
                            <option value="<?php echo $dep['id']; ?>"><?php echo $dep['name']; ?></option>
                        <?php } ?>
                    </select>
                </div>

                <div class="form-group">
                    <label for="provincia">Provincia</label>
                    <select id="provincia" name="provincia" onchange="cargarDistritos(this.value)">
                        <option value="" disabled selected>-- Esperando... --</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="distrito">Distrito (Obligatorio)</label>
                    <select id="distrito" name="id_distrito" required>
                        <option value="" disabled selected>-- Esperando... --</option>
                    </select>
                </div>
            </div>
            <div id="seccion_alumno" class="form-grid seccion-dinamica">
                <div class="seccion-titulo"><strong>Datos Académicos (Alumno)</strong></div>
                <div class="form-group">
                    <label for="cod_universitario">Código Universitario (Será su usuario) *</label>
                    <input type="text" id="cod_universitario" name="cod_universitario">
                </div>
                <div class="form-group">
                    <label for="id_escuela_alumno">Escuela Profesional / Facultad *</label>
                    <select id="id_escuela_alumno" name="id_escuela">
                        <option value="" disabled selected>-- Seleccione su Escuela --</option>
                        <?php foreach ($escuelas as $esc) { ?>
                            <option value="<?php echo $esc['id_escuela']; ?>"><?php echo $esc['nombre_escuela']; ?></option>
                        <?php } ?>
                    </select>
                </div>
                <div class="form-group">
                    <label for="anio_ingreso">Año de Ingreso</label>
                    <input type="number" id="anio_ingreso" name="anio_ingreso" min="1950" max="2026">
                </div>
                <div class="form-group">
                    <label for="ciclo">Ciclo Actual</label>
                    <select id="ciclo" name="ciclo">
                        <option value="" disabled selected>-- Seleccione su Ciclo --</option>
                        <option value="1">I</option>
                        <option value="2">II</option>
                        <option value="3">III</option>
                        <option value="4">IV</option>
                        <option value="5">V</option>
                        <option value="6">VI</option>
                        <option value="7">VII</option>
                        <option value="8">VIII</option>
                        <option value="9">IX</option>
                        <option value="10">X</option>
                        <option value="11">XI</option>
                        <option value="12">XII</option>
                    </select>
                </div>
            </div>

            <div id="seccion_personal" class="form-grid seccion-dinamica">
                <div class="seccion-titulo"><strong>Datos Laborales (Personal / Docente)</strong></div>
                <div class="form-group">
                    <label for="cod_admin">Código de Trabajador (Será su usuario) *</label>
                    <input type="text" id="cod_admin" name="cod_admin">
                </div>
                <div class="form-group">
                    <label for="cargo">Cargo / Especialidad</label>
                    <input type="text" id="cargo" name="cargo">
                </div>
                <div class="form-group">
                    <label for="condicion">Condición / Modalidad</label>
                    <select id="condicion" name="condicion">
                        <option value="" disabled selected>-- Seleccione su Condición --</option>
                        <option value="Nombrado">Nombrado</option>
                        <option value="Contratado">Contratado</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="categoria">Categoría / Nivel</label>
                    <input type="text" id="categoria" name="categoria">
                </div>
            </div>

            <div id="seccion_egresado" class="form-grid seccion-dinamica">
                <div class="seccion-titulo"><strong>Datos Académicos (Egresado)</strong></div>
                <div class="form-group">
                    <label for="cod_egresado">Código Universitario (Será su usuario) *</label>
                    <input type="text" id="cod_egresado" name="cod_egresado">
                </div>
                <div class="form-group">
                    <label for="id_escuela_egresado">Escuela Profesional / Facultad *</label>
                    <select id="id_escuela_egresado" name="id_escuela_egresado">
                        <option value="" disabled selected>-- Seleccione su Escuela --</option>
                        <?php foreach ($escuelas as $esc) { ?>
                            <option value="<?php echo $esc['id_escuela']; ?>"><?php echo $esc['nombre_escuela']; ?></option>
                        <?php } ?>
                    </select>
                </div>
                <div class="form-group">
                    <label for="anio_ingreso_egreso">Año de Ingreso</label>
                    <input type="number" id="anio_ingreso_egreso" name="anio_ingreso_egreso" min="1950" max="2026">
                </div>
                <div class="form-group">
                    <label for="anio_egreso">Año de Egreso</label>
                    <input type="number" id="anio_egreso" name="anio_egreso" min="1950" max="2026">
                </div>
            </div>

            <div id="seccion_institucion" class="form-grid seccion-dinamica">
                <div class="seccion-titulo"><strong>Datos de la Entidad (Persona Jurídica)</strong></div>
                <div class="form-group">
                    <label for="ruc">RUC (Será su usuario) *</label>
                    <input type="text" id="ruc" name="ruc" maxlength="11">
                </div>
                <div class="form-group">
                    <label for="razon_social">Razón Social *</label>
                    <input type="text" id="razon_social" name="razon_social">
                </div>
                <div class="form-group full-width">
                    <label for="direccion_inst">Dirección Fiscal *</label>
                    <input type="text" id="direccion_inst" name="direccion_inst">
                </div>
            </div>

            <button type="submit" class="btn-submit">COMPLETAR REGISTRO</button>
        </form>

        <div class="register-link">
            ¿Ya tienes una cuenta? <br><br> <a href="index.php">Ir a Iniciar Sesión</a>
        </div>
    </div>

    <script>
        function mostrarCampos() {
            var tipo = document.getElementById("tipo_usuario").value;

            // 1. Ocultar todo primero
            document.getElementById("seccion_personales").style.display = "none";
            document.getElementById("seccion_alumno").style.display = "none";
            document.getElementById("seccion_personal").style.display = "none";
            document.getElementById("seccion_egresado").style.display = "none";
            document.getElementById("seccion_institucion").style.display = "none";
            document.getElementById("seccion_ubicacion").style.display = "none"; // <-- Agregado

            // 2. Mostrar según tipo
            if (tipo === "1") {
                document.getElementById("seccion_personales").style.display = "grid";
                document.getElementById("seccion_alumno").style.display = "grid";
                document.getElementById("seccion_ubicacion").style.display = "grid"; // <-- Mostrar Ubigeo
            }
            else if (tipo === "2") {
                document.getElementById("seccion_personales").style.display = "grid";
                document.getElementById("seccion_personal").style.display = "grid";
                document.getElementById("seccion_ubicacion").style.display = "grid"; // <-- Mostrar Ubigeo
            }
            else if (tipo === "3") {
                document.getElementById("seccion_personales").style.display = "grid";
                document.getElementById("seccion_egresado").style.display = "grid";
                document.getElementById("seccion_ubicacion").style.display = "grid"; // <-- Mostrar Ubigeo
            }
            else if (tipo === "4") {
                document.getElementById("seccion_ubicacion").style.display = "grid";
                document.getElementById("seccion_institucion").style.display = "grid";
                // <-- AHORA SÍ SE MUESTRA PARA INSTITUCIÓN
            }
        }


        function cargarProvincias(id_dep) {
            const formData = new FormData();
            // CORREGIDO: Cambiamos 'id_departamento' por 'department_id'
            formData.append('department_id', id_dep);

            fetch('get_provincias.php', { method: 'POST', body: formData })
                .then(response => response.text())
                .then(html => {
                    document.getElementById('provincia').innerHTML = html;
                    document.getElementById('distrito').innerHTML = "<option value='' disabled selected>-- Esperando... --</option>";
                });
        }

        function cargarDistritos(id_prov) {
            const formData = new FormData();
            // CORREGIDO: Cambiamos 'id_provincia' por 'province_id'
            formData.append('province_id', id_prov);

            fetch('get_distritos.php', { method: 'POST', body: formData })
                .then(response => response.text())
                .then(html => {
                    document.getElementById('distrito').innerHTML = html;
                });
        }

    </script>
</body>

</html>
</file>

<file path="auth.php">
<?php
session_start();
if ($_SESSION["auth"]!="1") {  
header('location:index.php');
exit();
}
?>
</file>

<file path="index.php">
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceso | Mesa de Partes UNJFSC</title>
    <link rel="stylesheet" href="css/estilos.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;1,400&family=Montserrat:wght@300;400;500&display=swap"
        rel="stylesheet">


</head>

<body>
    <div class="overlay"></div>

    <div class="login-card">
        <div class="login-header">
            <h1>LOGIN</h1>
            <p>Universidad Nacional José Faustino Sánchez Carrión</p>
        </div>

        <?php
        // Mostrar errores si p_login.php nos devuelve aquí con algún problema
        if (isset($_GET['error'])) {
            if ($_GET['error'] == 'pass') {
                echo '<div class="alerta-error">Contraseña incorrecta. Intente nuevamente.</div>';
            } else if ($_GET['error'] == 'user') {
                echo '<div class="alerta-error">El usuario no está registrado.</div>';
            }
        }
        ?>

        <form action="p_login.php" method="POST">
            <div class="form-group">
                <label for="tipo_login">Tipo de Perfil</label>
                <select name="tipo_login" id="tipo_login" required>
                    <option value="" disabled selected>-- Seleccione --</option>
                    <option value="1">Persona Natural</option>
                    <option value="2">Alumno</option>
                    <option value="3">Personal</option>
                    <option value="4">Institución Externa</option>
                    <option value="5">Egresado</option>
                </select>
            </div>
            <div class="form-group">
                <label for="identificador">RUC / Código</label>
                <input type="text" id="identificador" name="identificador" required autocomplete="username"
                    placeholder="Ej: 12345678">
            </div>

            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" required>
            </div>

            <button type="submit" class="btn-submit">INGRESAR</button>

            <div style="text-align: right; margin-top: 25px;">
                <a href="login_mesa.php" onmouseover="this.style.color='#c5a059'" onmouseout="this.style.color='#666'"
                    style="color: #666; font-size: 0.85rem; text-decoration: none; border-bottom: 1px dashed #ccc; transition: color 0.3s ease;">
                    ¿Eres usuario Mesa de Partes?
                </a>
            </div>

        </form>

        <div class="register-link">
            ¿No tienes una cuenta? <br><br> <a href="registro.php">Solicitar Registro</a>
        </div>
    </div>
</body>

</html>
</file>

<file path="login_mesa.php">
<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceso Administrativo | Mesa de Partes UNJFSC</title>
    <link rel="stylesheet" href="css/estilos.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;1,400&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet">
</head>

<body>
    <div class="overlay"></div>

    <div class="login-card">
        <div class="login-header">
            <h1>MESA DE PARTES</h1>
            <p>Acceso exclusivo para personal administrativo</p>
        </div>

        <?php
        if (isset($_GET['error'])) {
            if ($_GET['error'] == 'pass') {
                echo '<div class="alerta-error" style="color:red; margin-bottom:10px; text-align:center;">Contraseña incorrecta.</div>';
            } else if ($_GET['error'] == 'user') {
                echo '<div class="alerta-error" style="color:red; margin-bottom:10px; text-align:center;">Usuario no encontrado o no tiene permisos.</div>';
            }
        }
        ?>

        <form action="p_login_mesa.php" method="POST">
            
            <div class="form-group">
                <label for="identificador">Usuario / Correo Institucional</label>
                <input type="text" id="identificador" name="identificador" required autocomplete="username" placeholder="Ej: admin / nombre@unjfsc.edu.pe">
            </div>

            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" required>
            </div>

            <button type="submit" class="btn-submit">INGRESAR AL SISTEMA</button>

            <div style="text-align: right; margin-top: 25px;">
                <a href="index.php" onmouseover="this.style.color='#c5a059'" onmouseout="this.style.color='#666'"
                    style="color: #666; font-size: 0.85rem; text-decoration: none; border-bottom: 1px dashed #ccc; transition: color 0.3s ease;">
                    ¿Eres usuario regular? Volver al inicio
                </a>
            </div>

        </form>
    </div>
</body>

</html>
</file>

</files>
